# Bugs
no errors when the load file has invalid parameters like R_serial (R_serial is no more in use)
export seems broken
remove get_main_dir and use app_settings to provide such information.



# Important features to add



# Nice to have
add definition list for all the terms used into the electraoacoustics file.
such as V_in, V_spk, R_sys, R_nom, etc.
better explanation when port is too small in diameter
System title in summary disappears but horizontal line stays

when motor definition type changes to Bl and Mmx, paste values from model into the widgets
link bl and re from mms type to mmd type - this changes the json files

add sine sweep time signal of displacements

make sine sweep example:
P = 48000
Q = 44100

offset = 2000
instfreq = np.exp(np.linspace(np.log(offset+100), np.log(offset+23900), 96000))-offset
deltaphase = 2*np.pi*instfreq/P
cphase = np.cumsum(deltaphase)
sig = np.sin(cphase)

auto update when any value in user_form changes


add:
- Peak displacement at 1020Hz: 0.0143 mm
- Peak displacement overall: 0.313 mm

from Claude
 Recommendations (naming, dead code, documented limits)
  6. No voice-coil inductance Le anywhere — impedance has no HF rise and SPL ignores Le. Already acknowledged in the impedance title; flagging in case HF accuracy matters.
